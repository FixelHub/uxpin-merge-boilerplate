import React from 'react';
import Button from '../Button';

export default (
  <Button
    uxpId='fds-button'
    type='primary'
    size='medium'
    icon='add'
    iconPosition='left'
    isFullWidth={false}
    disabled={false}
    onClick={() => {}}
  >
    button
  </Button>
);
