import React from 'react';
import RadioButton from '../RadioButton';

export default (
  <RadioButton
    uxpId='fds-radio-button'
    disabled={false}
    checked={false}
    name='radio-button-set'
    onChange={() => {}}
  />
);
