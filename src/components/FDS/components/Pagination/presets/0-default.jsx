import React from 'react';
import Pagination from '../Pagination';

export default (
  <Pagination
    uxpId='fds-pagination'
    showPagesInfo
    totalItems={100}
    itemsPerPage={10}
    currentPage={1}
    onChange={() => {}}
  />
);
