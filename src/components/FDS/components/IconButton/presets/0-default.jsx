import React from 'react';
import IconButton from '../IconButton';

export default (
  <IconButton
    uxpId='fds-icon-button'
    type='circle'
    size='medium'
    icon='brightness_1'
    disabled={false}
    onClick={() => {}}
  />
);
