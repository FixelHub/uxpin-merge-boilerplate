import React from 'react';
import Button from '../Button';
// import Button from '../Button';

export default (
  <Button
    type="primary"
    size="tiny"
    icon=""
    iconOnly={false}
    disabled={false}>
    Click Me!
  </Button>
);
