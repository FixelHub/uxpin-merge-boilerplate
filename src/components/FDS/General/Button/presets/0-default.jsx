import React from 'react';
import Button from '../Button.tsx';

export default (
  <Button
    type="primary"
    size="tiny"
    icon=""
    iconOnly={false}
    disabled={false}
    label="Hello Button"
  />
);
